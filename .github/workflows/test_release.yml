name: Upload Python Package to TestPyPI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  release:
    types: [created]

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install setuptools wheel twine
      - name: Build and publish
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: pypi-AgENdGVzdC5weXBpLm9yZwIkOWNlODI3ZTAtOGQ4NC00MTdkLWJlNzItYWIyMjEwNWU0Y2VhAAIqWzMsIjAyNmIzZWVhLTM5MzUtNDBiYy1hOWQ0LTM4ODQ2NDQxZTk3NCJdAAAGIER5DqqXxLTUgYKHDDx5AjXYvcWMDXg85rsbJ4X8IHhu
        run: |
          python3 -m build
          python3 -m twine upload --repository testpypi dist/* --verbose

#          python3 -m twine upload --repository testpypi dist/* --verbose
